# 配置历史记录

此文件只记录已废弃配置，不作为运行配置读取。

## 2026-09-23

- 废弃数据库地址：`43.136.32.239:3306`
- 原因：旧腾讯云服务器地址已过期。
- 当前生产配置：数据库与 OZLedger 后端同机，使用 `127.0.0.1:3306`。
- 数据库：MySQL，库名 `ozledger`。
- 处理：仓库示例配置、服务端默认值和部署文档统一改为 `127.0.0.1`。
- 数据影响：没有执行数据库迁移、删除或覆盖操作。

## 记录原则

过期地址、旧部署路径和旧运行方式只在这里保留历史说明；可执行代码和当前部署文档只保留当前配置。

